// Placeholder until WI-3 ports the Three.js scene from
// corlens/apps/web/src/fragments/Landing/scene/. Renders nothing so the
// Layout can already mount the Navbar + theme classes correctly.
export function GlobalStarfield(): null {
  return null;
}
